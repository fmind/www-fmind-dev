ENV=dev
# Cryptographic secret key used by FastHTML for session cookie signing.
# Generate a secure random value for production (e.g., using `openssl rand -hex 32`).
SESSION_SECRET=dev-secret-key-change-me-in-production
